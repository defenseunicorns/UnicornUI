import type { ScopedStyles } from '@uui';

export const scopedExample: ScopedStyles = {
  $self: {
    gap: '32px',
    width: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
  }
};
